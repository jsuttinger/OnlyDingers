{
  "name": "only-dingers",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Only Dingers — a PWA that shows MLB home runs.",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host"
  },
  "devDependencies": {
    "vite": "^5.4.10",
    "vite-plugin-pwa": "^0.20.5"
  }
}
